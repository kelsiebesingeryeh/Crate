import Survey from './Survey'

describe('Survey', () => {
  it('should exist', () => {
    expect(true).toEqual(true);
  });

  it.only('should exist', () => {
    expect(true).toEqual(true);
  });
});